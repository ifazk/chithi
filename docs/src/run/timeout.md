# Using with timeout command

The task runner does not support a timeout feature, so there may be a need to
use the `timeout` command with runner.

TODO: Recommend using `sh` for better session handling.
